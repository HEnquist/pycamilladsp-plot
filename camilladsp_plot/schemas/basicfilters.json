{
  "Gain": {
    "properties": {
      "type": {
        "enum": ["Gain"]
      },
      "gain": {
        "type": "number"
      },
      "inverted": {
        "type": "boolean",
        "default": false
      },
      "mute": {
        "type": "boolean",
        "default": false
      }
    },
    "required": ["type", "gain"]
  },
  "Delay": {
    "properties": {
      "type": {
        "enum": ["Delay"]
      },
      "delay": {
        "type": "number",
        "minimum": 0.0
      },
      "unit": {
        "enum": ["ms", "samples"],
        "default": "ms"
      }
    },
    "required": ["type", "delay"]
  },
  "Volume": {
    "properties": {
      "type": {
        "enum": ["Volume"]
      },
      "ramp_time": {
        "type": "number",
        "default": "200",
        "minimum": 0.0
      }
    },
    "required": ["type"]
  },
  "Loudness": {
    "properties": {
      "type": {
        "enum": ["Loudness"]
      },
      "ramp_time": {
        "type": "number",
        "default": "200.0",
        "minimum": 0.0
      },
      "reference_level": {
        "type": "number"
      },
      "high_boost": {
        "type": "number",
        "default": "10.0",
        "minimum": 0.0
      },
      "low_boost": {
        "type": "number",
        "default": "10.0",
        "minimum": 0.0
      }
    },
    "required": ["type", "reference_level"]
  }
}
